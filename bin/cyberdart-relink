#!/usr/bin/env bash
# cyberdart-relink — re-create skill symlinks based on skill_prefix config
#
# Usage:
#   cyberdart-relink
#
# Env overrides (for testing):
#   CYBERDART_STATE_DIR   — override ~/.cyberdart state directory
#   CYBERDART_INSTALL_DIR — override cyberdart install directory
#   CYBERDART_SKILLS_DIR  — override target skills directory
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CYBERDART_CONFIG="${SCRIPT_DIR}/cyberdart-config"

# Detect install dir
INSTALL_DIR="${CYBERDART_INSTALL_DIR:-}"
if [ -z "$INSTALL_DIR" ]; then
  if [ -d "$HOME/.claude/skills/cyberdart" ]; then
    INSTALL_DIR="$HOME/.claude/skills/cyberdart"
  elif [ -d "${SCRIPT_DIR}/.." ] && [ -f "${SCRIPT_DIR}/../setup" ]; then
    INSTALL_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"
  fi
fi

if [ -z "$INSTALL_DIR" ] || [ ! -d "$INSTALL_DIR" ]; then
  echo "Error: cyberdart install directory not found." >&2
  echo "Run: cd ~/.claude/skills/cyberdart && ./setup" >&2
  exit 1
fi

# Detect target skills dir
SKILLS_DIR="${CYBERDART_SKILLS_DIR:-$(dirname "$INSTALL_DIR")}"
[ -d "$SKILLS_DIR" ] || mkdir -p "$SKILLS_DIR"

# Read prefix setting
PREFIX=$("$CYBERDART_CONFIG" get skill_prefix 2>/dev/null || echo "false")

# Helper: remove old skill entry (symlink or real directory with symlinked SKILL.md)
_cleanup_skill_entry() {
  local entry="$1"
  if [ -L "$entry" ]; then
    rm -f "$entry"
  elif [ -d "$entry" ] && [ -L "$entry/SKILL.md" ]; then
    rm -rf "$entry"
  fi
}

# Discover skills (directories with SKILL.md, excluding meta dirs)
SKILL_COUNT=0
for skill_dir in "$INSTALL_DIR"/*/; do
  [ -d "$skill_dir" ] || continue
  skill=$(basename "$skill_dir")
  # Skip non-skill directories
  case "$skill" in bin|browse|design|docs|extension|lib|node_modules|scripts|test|.git|.github) continue ;; esac
  [ -f "$skill_dir/SKILL.md" ] || continue

  if [ "$PREFIX" = "true" ]; then
    # Don't double-prefix directories already named cyberdart-*
    case "$skill" in
      cyberdart-*) link_name="$skill" ;;
      *)        link_name="cyberdart-$skill" ;;
    esac
    # Remove old flat entry if it exists (and isn't the same as the new link)
    [ "$link_name" != "$skill" ] && _cleanup_skill_entry "$SKILLS_DIR/$skill"
  else
    link_name="$skill"
    # Don't remove cyberdart-* dirs that are their real name (e.g., cyberdart-upgrade)
    case "$skill" in
      cyberdart-*) ;; # Already the real name, no old prefixed link to clean
      *)        _cleanup_skill_entry "$SKILLS_DIR/cyberdart-$skill" ;;
    esac
  fi
  target="$SKILLS_DIR/$link_name"
  # Upgrade old directory symlinks to real directories
  [ -L "$target" ] && rm -f "$target"
  # Create real directory with symlinked SKILL.md (absolute path)
  mkdir -p "$target"
  ln -snf "$INSTALL_DIR/$skill/SKILL.md" "$target/SKILL.md"
  SKILL_COUNT=$((SKILL_COUNT + 1))
done

# Patch SKILL.md name: fields to match prefix setting
"$INSTALL_DIR/bin/cyberdart-patch-names" "$INSTALL_DIR" "$PREFIX"

if [ "$PREFIX" = "true" ]; then
  echo "Relinked $SKILL_COUNT skills as cyberdart-*"
else
  echo "Relinked $SKILL_COUNT skills as flat names"
fi
